import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#C70039 ",
    },
    secondary: {
      main: "#000000",
    },
    error: {
      main: "#EE2A74",
    },
    button: {
      main: "#DAF7A6 ",
    },
  },
});

export default theme;
